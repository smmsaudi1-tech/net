rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Allow public read access for all site visitors
    // Allow full upload/delete access for authenticated users
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
